import React from "react";

import "./loadingSpinner.css";

const LoadingSpinner = ({ classes }) => {
  return <div className={`loading-spinner ${classes}`}></div>;
};

export default LoadingSpinner;
